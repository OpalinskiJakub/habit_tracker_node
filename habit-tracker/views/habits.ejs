<!DOCTYPE html>
<html>
<head>
    <title>Habit Tracker</title>
    <link rel="stylesheet" href="<%= basePath %>/style.css" />
</head>
<body>
<h1>📋 Twoje nawyki</h1>

<form action="<%= basePath %>/habits/add" method="POST">
    <input type="text" name="name" placeholder="Nazwa nawyku" required />
    <button type="submit">➕ Dodaj</button>
</form>

<ul>
    <% habits.forEach(habit => { %>
        <li>
            <strong><%= habit.name %></strong><br/>
            Wykonano: <%= habit.count %> razy<br/>
            Ostatnio: <%= habit.lastDate %><br/>

            <a href="<%= basePath %>/habits/<%= habit.id %>"><button>➕ Wykonaj</button></a>

            <form action="<%= basePath %>/habits/delete/<%= habit.id %>" method="POST" style="display:inline;">
                <button type="submit">🗑️ Usuń</button>
            </form>
        </li>
    <% }) %>
</ul>

<h3>📂 Import danych JSON</h3>
<form action="<%= basePath %>/habits/import" method="POST" enctype="multipart/form-data">
    <input type="file" name="jsonFile" accept=".json" required />
    <button type="submit">Importuj</button>
</form>

<h3>💾 Eksport danych JSON</h3>
<form action="<%= basePath %>/habits/export" method="GET">
    <button type="submit">Eksportuj</button>
</form>

</body>
</html>
